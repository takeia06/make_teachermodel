[pytest]
# ここだけテスト対象にする
testpaths = tests

# GPUマークを正式登録（警告抑制）
markers =
    gpu: GPU tests

# 外部プロジェクトや巨大ディレクトリを再帰探索しない
norecursedirs =
    code
    mmdetection
    pretrained_ckpt
    data
    datasets
    .git
    venv
    .venv
    mmdetection.venv
